Options -Indexes

RewriteEngine On

# Allow direct file access (for future static assets)
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Route everything else through index.php
RewriteRule ^ index.php [QSA,L]
